[project]
name = "himalaya-worker-agent"
version = "0.1.0"
description = "Pulls jobs from himalaya-master and runs them through local llama-server"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.7",
    "pydantic-settings>=2.5",
    "tenacity>=9.0",
    "orjson>=3.10",
]
